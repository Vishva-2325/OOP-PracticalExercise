interface Classify {
    String getDivision(double avg);
}

class Result implements Classify {
    public String getDivision(double avg) {
        return avg >= 60 ? "First Division" : "Other";
    }
}
